# Render Blueprint for black-squid.
#
# Render Dashboard → Blueprints → New Blueprint Instance → point at this repo
# and pick this file. Render provisions the service; values marked `sync: false`
# are kept out of the blueprint on purpose so secrets never live in git — set
# them once in the dashboard.
#
# After the first deploy:
#   1. Note the auto-assigned URL (e.g. https://black-squid.onrender.com).
#   2. Set NEXT_PUBLIC_SITE_URL to that URL in the service env (or your custom
#      domain once attached) and trigger a redeploy.
#   3. Add the URL + /auth/confirm to Supabase Auth → URL Configuration.
#   4. Register https://<your-url>/api/stripe/webhook in Stripe → Webhooks,
#      copy the signing secret into STRIPE_WEBHOOK_SECRET.

services:
  - type: web
    name: black-squid
    runtime: node
    plan: starter
    region: oregon
    branch: main
    autoDeploy: true
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
      # The actual origin Render assigns / your custom domain. Set after the
      # service is created so the value matches the Render-assigned host.
      - key: NEXT_PUBLIC_SITE_URL
        sync: false
      # Supabase project credentials.
      - key: NEXT_PUBLIC_SUPABASE_URL
        sync: false
      - key: NEXT_PUBLIC_SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      # Stripe (live keys for production, test keys for a staging service).
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        sync: false
      - key: EZSTEMZ_STRIPE_PRICE_ID
        sync: false
      # KitForge price id — leave unset until the KitForge Stripe product exists.
      - key: KITFORGE_STRIPE_PRICE_ID
        sync: false
      # Set this AFTER you've registered the live webhook endpoint in Stripe
      # against the Render-assigned URL — the signing secret is per-endpoint
      # and not known until the webhook is created.
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      # EZStemz release repo hosting the signed artifacts (DMG + .exe).
      # (EZSTEMZ_GITHUB_REPO overrides this if set.)
      - key: EZSTEMZ_GITHUB_REPO
        value: orange-groove/ez-stemz
      # KitForge release repo — leave unset until its installers are published.
      - key: KITFORGE_GITHUB_REPO
        value: orange-groove/kitforge
      # Shared token for reading private release repos. Fine-grained PAT with
      # `Contents: read` on the repo(s). Override per-product with
      # EZSTEMZ_GITHUB_TOKEN / KITFORGE_GITHUB_TOKEN only if needed.
      - key: GITHUB_TOKEN
        sync: false